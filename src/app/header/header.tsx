import AuthButtons from "../../components/authButtons";

const Header = () => {
    return (
        <nav className="flex w-full p-5">
            <AuthButtons/>
        </nav>
    )
}

export default Header